# 🎓 Увод в Secure Code Programming (Сигурно програмиране)

> Подготвено за курса **„Писане на сигурен код“**
> Бургаски държавен университет, проф. д-р Асен Златаров
> © 2025 — За академично ползване с цитиране на източника

---

## 📖 Въведение

Сигурното програмиране е дисциплина, която обединява **инженерните практики, информационната сигурност и архитектурното мислене**.
Неговата цел е проста — **кодът да остане защитен дори когато бъде атакуван**.

Всеки програмист днес е и потенциален „пазител“ на информационната сигурност.
Кодът, който пишем, може да предпази или да застраши цяла организация.

---

## 🧩 Част I — Съвременен контекст и мотивация

### 1. Необходимост от сигурни системи

* През последното десетилетие **90% от уязвимостите** произлизат от грешки в кода или конфигурации.
* Зависимостите и отвореният код увеличават риска — злонамерените пакети, supply-chain атаки и компрометирани CI/CD среди стават все по-чести.
* Някои от най-известните пробиви (Equifax, SolarWinds, Log4Shell) започват от дребни пропуски в кода.

**Ключово правило:**

> Сигурността е качество, а не допълнение. Ако кодът не е сигурен, той не е „добър код“.

---

### 2. Проактивен процес на разработка

Съвременните подходи заменят модела „накрая ще направим security test“ с **интегрирана сигурност през целия жизнен цикъл** (Secure SDLC).

Етапи на сигурния жизнен цикъл:

1. **Планиране** — дефинирай изисквания за сигурност.
2. **Проектиране** — моделирай заплахите и избери подходящи защити.
3. **Имплементация** — използвай сигурни библиотеки и API.
4. **Тестване** — извършвай SAST, DAST, fuzzing, code reviews.
5. **Поддръжка** — актуализирай зависимости и управлявай уязвимости.

**DevSecOps** е практическата реализация:

> “Security is everyone’s responsibility.”
> Всеки push преминава през автоматични security проверки.

---

### 3. Принципи за сигурно проектиране

| Принцип               | Обяснение                                                     |
| --------------------- | ------------------------------------------------------------- |
| **Least Privilege**   | Давай минимални възможни права.                               |
| **Defense in Depth**  | Защита на много нива — код, инфраструктура, достъп.           |
| **Fail Securely**     | При грешка системата трябва да остане защитена.               |
| **Secure by Default** | Настройките по подразбиране са безопасни.                     |
| **Open Design**       | Без „тайни“ в алгоритъма — само ключовете са тайна.           |
| **Zero Trust**        | Всяко взаимодействие се проверява — дори вътрешни компоненти. |
| **Auditability**      | Всички важни действия се записват и могат да се проследят.    |

---

### 4. Моделиране на заплахи (Threat Modeling)

**Цел:** да се идентифицират уязвимости преди да бъдат експлоатирани.

**Процес (Microsoft STRIDE):**

* **S**poofing — измама на идентичност;
* **T**ampering — промяна на данни;
* **R**epudiation — невъзможност за доказване на действие;
* **I**nformation Disclosure — изтичане на данни;
* **D**enial of Service — отказ на услуга;
* **E**levation of Privilege — придобиване на неоторизирани права.

Изход: диаграма на потока на данни (DFD) с потенциални заплахи и контроли.

---

## ⚙️ Част II — Техники за сигурно програмиране

### 5. Sandbox и изолация

**Sandbox** е защитена среда, която изолира кода от системата:

* **iOS и Android**: всяко приложение работи в собствен UID, с отделно файлово пространство и ограничен достъп до API.
* **Browser sandbox (Chrome, Safari)**: всеки таб е отделен процес.
* **Docker / LXC**: контейнеризация за контролирани среди.

👉 *Цел:* ако приложението бъде компрометирано, щетата остава ограничена.

---

### 6. Валидиране на входни данни

„**Всички входни данни са зли**“ — това е първото правило на сигурното програмиране.

**Какво е вход?**

* Форма, query параметър, cookie, JSON, header, файл, API от друга услуга.

**Правилен подход:**

* Използвай *whitelist* (разрешени стойности) вместо *blacklist*.
* Определи тип, дължина, формат, граници.
* В Java Spring Boot — използвай `@Valid`, `@Pattern`, `@Email`, `@Min`, `@NotNull`.
* Regex валидация трябва да е внимателна — избягвай сложни конструкции, които могат да доведат до **ReDoS** (Regular Expression Denial of Service).

---

### 7. Достъп и удостоверяване

#### Сертификати вместо пароли

* Използвай **X.509 клиентски сертификати** и **mutual TLS (mTLS)**;
* Удостоверяването с ключове елиминира нуждата от съхранение на пароли;
* Ако сертификатът се пази локално — **криптирай го с парола** (напр. `.pfx` файл);
* Паролата се пази в secure store, не в кода.

#### Zero Trust модел

* Всеки потребител, услуга и устройство се валидира при всяка заявка;
* Не вярвай на вътрешни мрежи — прилагай същите политики и там.

---

### 8. Криптография и тайни

| Цел                   | Препоръчителни алгоритми     | Да се избягват  |
| --------------------- | ---------------------------- | --------------- |
| Хеширане              | SHA-256, SHA-3, BLAKE2       | MD5, SHA1       |
| Симетрично криптиране | AES-GCM, ChaCha20            | DES, RC4        |
| Асиметрично           | RSA ≥ 2048, ECC (Curve25519) | DSA, RSA < 1024 |
| Пароли                | bcrypt, Argon2               | SHA1, PBKDF1    |

**Правила:**

* Използвай само утвърдени библиотеки (`cryptography`, `OpenSSL`, `pyca`).
* Никога не изобретявай собствена криптография.
* **Ключове и сертификати не се пазят в Git.**
* Защитавай ги с:

  * **AWS Secrets Manager**, **Azure Key Vault**, **GCP Secret Manager**;
  * **Windows DPAPI / SecureString**;
  * **Linux Keyring или GPG**.

**Windows SecureString пример (C#):**

```csharp
SecureString s = new SecureString();
foreach (char c in "MySecret123") s.AppendChar(c);
```

---

### 9. Бази данни и достъп

* Не се свързвай с базата като `sa` или `root`.
  Използвай ограничен потребител само за нужните операции.
* Винаги използвай **prepared statements**:

  ```python
  cur.execute("SELECT * FROM users WHERE name = ?", (user,))
  ```
* Никога не изграждай SQL чрез конкатенация.
* Внимавай с **UTF-8 кодировки** и URL енкодинг, които могат да заобиколят филтри.

---

### 10. Уязвимости от зависимости

Дори безопасен код може да стане уязвим, ако библиотеките му са компрометирани.

**Примери:**

* `event-stream` в npm;
* `log4j` (Log4Shell);
* злонамерени пакети в PyPI.

**Предпазване:**

* Сканиране с `pip-audit`, `safety`, `npm audit`, `dependabot`.
* Фиксирай версии (`requirements.txt`, `package-lock.json`).
* Използвай доверени огледала (mirrors).

---

### 11. Облачен модел на сигурност (AWS / Azure)

**Принцип:**

> „Всичко е забранено, освен ако не е изрично разрешено.“

* AWS IAM Policies и Azure RBAC определят достъпа до ресурси;
* Използвай минимални права: вместо `AdministratorAccess`, дай само `s3:GetObject`;
* Създавай отделни роли за различни среди (dev/test/prod);
* Security Groups и Network ACLs — допълнителни слоеве на защита.

---

## 🔍 Част III — Тестове и верификация на сигурността

### Security Testing

* **SAST (Static Analysis Security Testing)** — анализ на кода без изпълнение (напр. Bandit, SonarQube).
* **DAST (Dynamic Analysis Security Testing)** — тестване на работещото приложение (напр. OWASP ZAP).
* **IAST / Fuzzing** — откриване на уязвимости при изпълнение с произволни входове.
* **Dependency scanning** — търсене на известни уязвимости в зависимостите.

### Security Code Review

* Проверка за SQL Injection, XSS, командно изпълнение, невалидирани входове.
* Проверка на логика и управление на грешки.
* Преглед на криптографските практики и API използване.

---

## 🧱 Част IV — Secure Deployment & Privacy

### Secure Installation

* Минимални разрешения за инсталационните скриптове.
* Проверка на хешове и подписи на пакети.
* Автоматично създаване на защитени директории и ключове при първо стартиране.

### Privacy by Design

* Минимизиране на събираните лични данни;
* Ясни правила за достъп и съхранение;
* Възможност за изтриване по искане на потребителя;
* Прозрачност при обработка на данните (GDPR съвместимост).

---

## 💡 Част V — Обобщение и добри практики

| Добра практика                      | Цел                               |
| ----------------------------------- | --------------------------------- |
| Валидирай всички входове            | Предотвратяване на XSS, SQLi, RCE |
| Разделяй права и ресурси            | Минимизиране на щети              |
| Използвай силна криптография        | Поверителност и интегритет        |
| Следи зависимостите                 | Защита от supply-chain атаки      |
| Използвай sandbox и контейнеризация | Изолация и ограничаване на щети   |
| Приложи Zero Trust                  | Постоянна верификация на достъп   |
| Автоматизирай сигурността           | Непрекъснат контрол в CI/CD       |

---

## 🏁 Заключение

Сигурното програмиране не е набор от правила, а **начин на мислене**.
То изисква разбиране, култура и постоянство.

> “Кодът не е сигурен, защото никой не го е нападнал — той е сигурен, защото няма да успеят, когато го направят.”

---

### 📚 Препоръчителна литература

* *Writing Secure Code* — Michael Howard & David LeBlanc (Microsoft Press)
* OWASP Secure Coding Practices Checklist
* NIST SP 800-53 & SP 800-218 (SSDF)
* Microsoft SDL
* Google Cloud & AWS Well-Architected Security Frameworks

---

© 2025 Бургаски държавен университет, проф. д-р Асен Златаров
Разрешено за академично използване с цитиране на източника.
Commercial use prohibited.
